/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   ft_atoi_main.c                                     :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: harumoren2507 <kuroshiro0725@gmail.com>    +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2024/01/28 03:53:12 by harumoren25       #+#    #+#             */
/*   Updated: 2024/01/28 05:10:11 by harumoren25      ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include <stdio.h>

int ft_atoi(char *str)
{
    int sign;
    int result;

    sign = 1;
    result = 0;

    while (*str == ' ' || ((*str >= 9) && (*str <= 13)))
        str++;
    while ((*str == '+') || (*str == '-'))
    {
        if (*str == '-')
        {
            sign *= -1;
        }
        str++;
    }
    while ('0' <= *str && *str <= '9')
    {
        result = result * 10 + (*str - '0');
        str++;
    }
    return (result*sign);
}


int main(int argc, char* argv[])
{
    if (argc != 2)
    {
        printf("%s\n", argv[0]);
        return 1;
    }
    int result = ft_atoi(argv[1]);
    printf("%d\n", result);
    return 0;
}

/*

*コマンドライン引数の説明

*(int argc, char *argv[])

*argc (Argument Count): 
コマンドラインを通じてプログラムに渡される引数の数を表します。
これにはプログラム名自体も含まれます。

たとえば、./program arg1 arg2 を実行すると、argc は 3 になります（program, arg1, arg2）。

*argv (Argument Vector): 
char *argv[] は文字列の配列で、
具体的にはコマンドライン引数を指します。

各要素 (argv[0], argv[1], ...) は、
コマンドライン上の各引数を指す文字列へのポインタです。

argv[0] は常にプログラム自体の名前を指し、argv[1] は最初の引数を指します。




*argv[] のイメージ

もちろんです。別の簡単な例で説明します。
今度は「誕生日パーティーの招待状」を例にとりましょう。

*誕生日パーティーの招待状 /招待状のリスト = argv[]:

誕生日パーティーにはたくさんのゲストが招待されます。
ゲストそれぞれに送る招待状のリストを想像してください。
これが argv[]、つまりコマンドライン引数の配列です。
各招待状 = 文字列:

各招待状にはゲストの名前やパーティーの詳細が書かれています。
これは一つ一つの文字列です。たとえば、「Aliceへ、金曜日の7時に私の家でパーティーがあるよ！」というようなメッセージです。

*招待状のリストにある各招待状への参照 = ポインタ (*argv[]):

招待状のリストには、実際の招待状ではなく、それぞれの招待状への参照（つまり「Aliceの招待状はここにある」、
「Bobの招待状はそこにある」といった情報）が含まれています。これらの参照がポインタです。

*コマンドライン引数としての招待状のリスト
プログラムが実行されるとき、あなたはコマンドラインを通してプログラムに「招待状のリスト」を渡します。
たとえば、./party Alice Bob Charlie のように実行すると、Alice、Bob、Charlieそれぞれの招待状がリストに含まれます。

プログラムの main 関数はこの「招待状のリスト」を受け取ります。
argv[0] はプログラム自体（この場合は ./party）、argv[1] は最初のゲスト（Aliceの招待状への参照）、

argv[2] は次のゲスト（Bobの招待状への参照）、という具合になります。

この例で言えば、argv は招待状のリストを表し、各 argv[x] は特定のゲスト（文字列）の招待状への参照（ポインタ）を表します。
プログラムはこのリストを通じて各ゲストの招待状（実際の文字列データ）にアクセスすることができます。


*/