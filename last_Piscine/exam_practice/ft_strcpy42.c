char    *ft_strcpy(char *dest, char *src)
{
        char*   start_dest;
        start_dest = dest;
        while (*src)
        {
                *dest = *src;
                src++;
                dest++;
        }
        *dest = '\0';
        return (dest);
}

/*
 strcpy関数はC言語の標準ライブラリに含まれ、ある文字列を別の場所にコピーするために使用されます。この関数の特徴は、ソース文字列srcから目的地のバッファdestに文字列をコピーする際、末尾のヌルバイト（\0）も含めてコピーすることです。ヌルバイトはC言語における文字列の終わりを表す特殊な文字で、文字列を扱う多くの関数がこのヌルバイトを文字列の終端として認識します。

提供されたft_strcpy関数のコードは、strcpy関数の動作を再現しています。while (*src)ループは、srcが指すソース文字列を一文字ずつdestにコピーしていきます。このループが*src、つまりsrcが指す現在の文字がヌルバイト（値が0）でない限り続けられる理由は、文字列の終端に達するまでコピー処理を行う必要があるからです。ヌルバイトに達した時点で、文字列の終わりと判断し、ループを終了します。

ループの中で、各イテレーションごとに*dest = *src;により現在の文字がコピーされ、その後srcとdestのポインタがそれぞれ次の文字を指すようにインクリメントされます。ループが終了した後、*dest = '\0';により、コピー先の文字列がヌルバイトで終了されることを保証します 。これにより、コピーされた文字列が正しく終端され、C言語の標準に沿った文字列として扱われます。

strcpy関数を使用する際には、destが指すバッファがコピーされる文字列を格納するのに十分な大きさであることを確認する必要があります。そうでなければ、バッファオーバーランというセキュリティリスクが生じ、プログラムが不正にメモリを上書きしてしまう可能性があります。

まとめると、while (*src)はソース文字列の終端に達するまで文字をコピーし続けるために必要であり、これによりstrcpyの仕様に沿った正確な文字列コピーが実現されます。
*/
